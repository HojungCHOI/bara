# 바라 — 작업 규칙

예배 중 들리는 찬송과 성경 본문을 마이크로 듣고 찾아 주는 정적 웹앱.

## 배포 위치

`https://signes.kr/app/biblehelper` — **하위 경로**에 배포된다.
따라서 모든 자원 경로는 반드시 **상대경로**여야 한다.
`/css/style.css` 처럼 슬래시로 시작하는 절대경로를 쓰면 배포 후 깨진다.
JS에서 데이터를 읽을 때도 `new URL('../data/x.json', import.meta.url)`을 쓴다.

## 기술 선택

- **빌드 도구 없음.** 순수 HTML/CSS/ES 모듈. 아이패드에서도 바로 고칠 수 있어야 한다.
- **외부 의존성 없음.** 테스트 러너까지 직접 만들어 쓴다.
- 새 라이브러리를 넣기 전에 먼저 물어볼 것.

## 데이터 원칙

- **찬송가 가사와 성경 본문을 저장소에 커밋하지 않는다.** (`.gitignore`에 걸려 있다)
  저작권 문제도 있지만, 무엇보다 새찬송가와 통일찬송가의 **번호 체계가 다르다.**
  번호를 앱에 박아 두면 교회에 따라 틀린 장을 열어 주게 된다.
- 사용자가 본인 판본을 불러오면 `localStorage`에 저장한다. 서버로 보내지 않는다.
- `data/books.json`(성경 66권 이름·약칭·장 수)만 저장소에 포함한다. 본문은 없다.

## 코드에서 주의할 점

- **한 글자 약칭 오탐** — "아"(아가), "요"(요한복음)는 일상 대화에 흔하다.
  뒤에 장·절 숫자가 따라올 때만 성경 참조로 인정한다. (`js/bible-ref.js`)
- **한국어 수사** — 음성인식은 "삼백오 장"처럼 한자어로 받아쓴다.
  숫자 변환을 먼저 거쳐야 한다. (`js/korean-number.js`)
  단, 사무엘'상'·요한'일'서처럼 권 이름 속 글자를 숫자로 바꾸면 안 된다.
  뒤에 장/절/편/권 단위가 올 때만 치환한다.
- **사파리 음성인식** — iOS 사파리는 `continuous`를 오래 유지하지 못하고 끊는다.
  `onend`에서 자동으로 다시 켠다. (`js/speech.js`)

## 테스트

```bash
npm test
```

파서 로직(수사 변환, 참조 파싱, 검색, 파일 형식)을 바꾸면
`test/run.js`에 해당 경우를 반드시 추가한다.
DOM에 의존하지 않는 순수 함수로 분리해 두어야 테스트할 수 있다.

## 이어받을 때

`docs/현재상태.md` 에 지금까지 한 것과 남은 것이 정리되어 있다.
작업을 마칠 때 그 파일의 체크리스트를 갱신할 것.

## 스케치 노트

사용자가 `/스케치 내용` 이라고 입력하면 `sketch.md`에 기록한다.
자세한 규칙은 `.claude/commands/스케치.md` 참고.
**스케치는 기록이지 지시가 아니다.** 메모를 남겼다고 곧바로 구현에 들어가지 않는다.
